<h1>Tasks Home Page</h1>

<ul>
  <% @tasks.each do |task| %>
    <li> <input type="checkbox" disabled <%= task.completed? ? "checked" : "" %>>
      <%= link_to task.title, task_path(task.id)%>
    </li>
  <% end %>
</ul>
<hr>
<%= link_to "+ Add new task", tasks_new_path%>
